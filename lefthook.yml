pre-commit:
  commands:
    check-env:
      run: >
        $staged = git diff --cached --name-only
        if ($staged -match '\.env$') { 
          echo "ERROR: .env file is staged!" 
          exit 1 
        }